{
  "title": "SOAP component",
  "description": "Generic SOAP / WebServices integration component",
  "credentials": {
    "fields": {
      "auth": {
        "required": false,
        "viewClass": "HTTPAuthView"
      },
      "wsdlURI": {
        "label": "WSDL URI",
        "required": true,
        "viewClass": "TextFieldWithNoteView",
        "placeholder": "http://www.webservicex.net/CreditCard.asmx?wsdl",
        "note": "Please specify a full URI of your WSDL, it should also be accessible from the internet."
      }
    }
  },
  "triggers": {
    "callTrigger": {
      "title": "Call",
      "main": "actions.CallAction",
      "fields": {
        "operation": {
          "viewClass": "SelectView",
          "label": "Operation",
          "required": true,
          "model": "getOperations",
          "prompt": "Chose an Operation [Service/Port]"
        }
      }
    }
  },
  "actions": {
    "callAction": {
      "title": "Call",
      "main": "triggers.CallAction",
      "fields": {
        "operation": {
          "viewClass": "SelectView",
          "label": "Operation",
          "required": true,
          "model": "getOperations",
          "prompt": "Chose an Operation [Service/Port]"
        }
      }
    }
  }
}

